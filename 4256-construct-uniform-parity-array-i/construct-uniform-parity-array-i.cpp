class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        //odd-odd=even
        //even-even=even
        //even-odd=odd

        // if there is atleast one odd then it can make every even present odd
        //if there is no odd then it is an array with only even values 
        //hence always true


        return true;


    }
};